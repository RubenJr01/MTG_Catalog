import { Router } from 'express';

const router = Router();

// TODO: Implement collection routes
// GET /collection - Get user's collection
// POST /collection - Add card to collection
// PUT /collection/:id - Update card in collection
// DELETE /collection/:id - Remove card from collection

export default router;
